version: "3"

services:
  # mongo:
    # image: mongo:latest
    # container_name: mongo
    # environment:
      # MONGO_INITDB_ROOT_USERNAME: ${MGUSER}
      # MONGO_INITDB_ROOT_PASSWORD: ${MGPASSWORD}
    # ports:
      # - "0.0.0.0:27017:27017"
    # networks:
      # - MONGO
    # volumes:
      # - MONGO_DATA:/data/db
      # - MONGO_CONFIG:/data/configdb
  # mongo-express: #TODO: remove for prod
    # image: mongo-express:latest
    # container_name: mongo-express
    # environment:
      # ME_CONFIG_MONGODB_ADMINUSERNAME: ${MGUSER}
      # ME_CONFIG_MONGODB_ADMINPASSWORD: ${MGPASSWORD}
      # ME_CONFIG_MONGODB_SERVER: mongo
      # ME_CONFIG_MONGODB_PORT: "27017"
    # ports:
      # - "0.0.0.0:8081:9081"
    # networks:
      # - MONGO
    # depends_on:
      # - mongo
  front:
    build: 
      context: ./app/frontend
    container_name: app-front
    ports:
      - "9082:80"



# networks:
#   MONGO:
#     name: MONGO

# volumes:
#   MONGO_DATA:
#     name: MONGO_DATA
#   MONGO_CONFIG:
#     name: MONGO_CONFIG